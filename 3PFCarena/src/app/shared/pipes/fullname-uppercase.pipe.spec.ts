import { FullnameUppercasePipe } from './fullname-uppercase.pipe';

describe('FullnameUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new FullnameUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
